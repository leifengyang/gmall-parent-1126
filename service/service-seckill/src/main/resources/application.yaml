server:
  port: 16000

spring:
  datasource:
    url: jdbc:mysql://192.168.200.130:3306/gmall_activity?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  redis:
    host: 192.168.200.130
    password: Lfy123!@!
    lettuce:
      pool:
        max-active: 16
        min-idle: 0
  rabbitmq:
    host: 192.168.200.130
    port: 5672
    username: admin
    password: admin
    virtualHost: /
    publisher-returns: true
    #发送端确认；
    publisher-confirm-type: correlated
    #手动ack机制
    listener:
      type: simple
      simple:
        acknowledge-mode: manual  #手动确认模式。
